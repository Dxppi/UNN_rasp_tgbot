services:
  bot:
    build: .
    container_name: rasp_bot
    env_file: .env
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/database.sqlite
    restart: unless-stopped
    tty: true

